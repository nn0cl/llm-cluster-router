display_name: LLM Cluster Router
short_description: Route AI tasks across Ollama, OpenAI, Anthropic, and Codex providers.
default_prompt: Use LLM cluster routing for this task. Check provider status, choose the best configured provider for the requested model, and write output directly to the allowed project path.
